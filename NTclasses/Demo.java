/* package whatever; // don't place package name! */

import java.util.*;
import java.lang.*;
import java.io.*;

/* Name of the class has to be "Main" only if the class is public. */
class Demo
{
	public static void main (String[] args) throws java.lang.Exception
	{
		Arrays.sort(ar);
for(k=0;k<ar.length-2;k++) {

int I=k+1, j=ar.lenghth-1;
while(I<j)
{
   if(ar[k]+ar[I]+ar[j]==n)
   {
      System.out.println(ar[k]+" "+ar[I]+" "ar[j]);
      I++;  j++;
   }
  else if(ar[I]+ar[j]<n)
   {
      I++;
   }
  else
   {
     j--;
   }
}
}
    